---
title: "component-alps-ec11l1525g01"
description: "Resolve the exact ALPS EC11L1525G01 rotary encoder used on Board L. Use for quadrature phase order, detents, switch terminals, mechanical drawing, soldering, lifetime, lifecycle uncertainty, and encod..."
sidebar_label: "component-alps-ec11l1525g01"
generated: true
---

# ALPS EC11L1525G01

Run the central validator and read all local JSON before using this record. The retained specification is manufacturer-authored but hosted by a mirror, so every specification claim is intentionally `UNVERIFIED`/`UNSOURCED`. The current ALPS product page was inaccessible and cannot substantiate lifecycle status. A 2026-08-02 retry confirms this is not a generic bot block: the sibling EC11E15244G1 detail page loads normally with the same client, but EC11L1525G01 returns HTTP 403 on every path (including the `alps.com` legacy redirect target) and no `EC11L` series appears in any current encoder sub-category listing — the exact model looks to have been withdrawn from Alps Alpine's current online catalog.

The retained mirror identifies 15 pulses and 30 detents, while the generator display says “15-detent.” Treat this as an UNSOURCED documentation discrepancy until manufacturer-primary bytes are locked. A and B are phase contacts referenced to common C; D/E are the momentary push-switch terminals and are unused on this board. Pins 6/7 are grounded mechanical/electrical support lugs in the project footprint.

Use the retained chatter, bounce, torque, life, shaft, panel-support, and solder-process facts only with their stated conditions. Firmware debounce, direction convention at the UI, installed knob load, panel support, switch use, lifecycle status, and bench behavior remain open.

The design's real operating point differs from every retained spec condition: V3P3's 10 kOhm pull-up (R22/R23) gives roughly 0.33 mA closed-contact current versus the retained 5 V/5 kOhm chatter-bounce test circuit, and the only retained minimum-current spec (500 uA) belongs to the unused D/E push switch, not the A/B/C rotary contacts. C22/C23 100 nF at ENC_A/ENC_B discharge through R27/R28 100 Ohm at roughly 33 mA peak with no retained contact-current rating. Bench debounce/contact verification at the real 3.3 V/10k operating point is required before this domain closes.

## Human component reference

Human projection of this bundle: [rec-c2991196](/docs/components/records/c2991196/). Those pages are generated from the JSON files here and add nothing to them — where the two disagree, this bundle is correct. See also the [component catalog](/docs/components/catalog/) and the [cross-component rules](/docs/components/integration/).